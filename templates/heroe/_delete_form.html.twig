<form method="post" action="{{ path('app_heroe_delete', {'id': heroe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ heroe.id) }}">
    <button class="btn">Delete</button>
</form>
