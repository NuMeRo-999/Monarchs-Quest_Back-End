<form method="post" action="{{ path('app_save_slot_delete', {'id': save_slot.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ save_slot.id) }}">
    <button class="btn">Delete</button>
</form>
